{
  "name": "brothers-protocol-cli-mvp",
  "version": "0.6.0",
  "main": "dist/cli.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "npm run build && node --test tests/*.test.mjs",
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "smoke": "npm run build && bash scripts/smoke.sh"
  },
  "keywords": ["ai", "agents", "orchestration", "cli", "task-management", "relay", "handoff", "llm"],
  "author": "avtogost77-gif",
  "license": "MIT",
  "description": "Markdown-first CLI for safe task handoff between AI agents — Relay Baton protocol",
  "private": false,
  "type": "module",
  "bin": {
    "brothers": "dist/cli.js"
  },
  "dependencies": {
    "commander": "^14.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
